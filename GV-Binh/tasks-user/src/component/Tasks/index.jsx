import React from 'react';
import PropTypes from 'prop-types';

const Tasks = props => {
    return (
        <div>
            Đây là component Task
        </div>
    );
};

Tasks.propTypes = {
    
};

export default Tasks;