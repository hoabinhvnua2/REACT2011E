import React from 'react';
import PropTypes from 'prop-types';

const Users = props => {
    return (
        <div>
            Đây là componet user
        </div>
    );
};

Users.propTypes = {
    
};

export default Users;